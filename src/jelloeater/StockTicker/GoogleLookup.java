package jelloeater.StockTicker;

import java.io.IOException;


public class GoogleLookup extends StockInfoLookup{ //TODO remove extends (it's only for debugMode)
	
	/*
	public static String price(String symbol) throws IOException {
		String quertyString=GoogleTickerData.getGoogleJSONfromWeb(symbol); // Gets JSON data from web
	    String priceString = GoogleTickerData.jsonToObjectVars(quertyString); // Takes JSON Give price
	    return priceString;
	}
	*/

}

